//console.log('ola mundo');
let amigos = ['rafael','jesus','jaime','irani','luciana'];
//console.log(amigos.length);

/*

for (let i = 0; i < amigos.length; i++){
     console.log(`Nome: ${amigos[i]}`); 
  }

  //templeate string (interpolação)
  console.log('Amigos: $(amigos[i})    ')
  
  

  // foreach For = para each = cada
  amigos.forEach(function( valor, indice){

   console.log('${indice} : ${valor}');

  });

  
  amigos.map(function(valor, indice){
    console.log('${indice}: ${valor}');

  });
 */

 /* Map

  vetor.map(function(val, val2){
    console.log(val2);
  });
*/  


